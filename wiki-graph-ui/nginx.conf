server {
    listen 3000 default_server;
    listen [::]:3000 default_server;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Security headers — Cloudflare sits in front for public, Caddy for LAN.
    add_header X-Content-Type-Options nosniff;
    add_header Referrer-Policy same-origin;

    # Cloudflare's "Cache Everything" rule ignores origin Cache-Control on
    # static assets — these standardised CDN directives are honored instead.
    # We rely on per-deploy ?v=BUILD_ID busting; tell CF to never cache.
    add_header CDN-Cache-Control "no-store";
    add_header Cloudflare-CDN-Cache-Control "no-store";
    etag on;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # No-cache config.js so PUBLIC_API_URL substitutions always win.
    location = /config.js {
        add_header Cache-Control "no-store";
        add_header CDN-Cache-Control "no-store";
        add_header Cloudflare-CDN-Cache-Control "no-store";
    }

    location = /health {
        access_log off;
        default_type application/json;
        return 200 '{"status":"ok"}';
    }
}
