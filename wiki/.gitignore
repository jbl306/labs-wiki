# hot.md is regenerated every mine (~hourly + on-demand).
# Keep git history clean per proposed-answer Part 5 Q2:
# "commit only daily snapshot, .gitignore the hourly rewrites."
#
# The committed daily snapshot lives at wiki/meta/hot-snapshot.md
# (written by scripts/snapshot_hot.py via a daily cron).
hot.md
